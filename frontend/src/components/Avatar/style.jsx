import styled from "styled-components";

export default styled.div`
  border-radius: 0.7em;
  display: flex;
  align-items: center;

  img {
    width: 2.3em;
    aspect-ratio: 1;
    border-radius: 50%;
    padding-right: 0.8rem;
  }
`;
